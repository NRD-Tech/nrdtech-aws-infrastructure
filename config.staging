# NOTE: These are only set for the staging environment
# NOTE: You should set the actual RDS_MASTER_PASSWORD_STAGING environment variable in a Secret in GitHub or Bitbucket
export RDS_MASTER_PASSWORD=$RDS_MASTER_PASSWORD_STAGING
